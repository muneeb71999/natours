extends baseEmail

block content
  p= `Hi ${firstName},` 
  p Forgot Password ? Click the link below to reset your password 
    a(href=url)=url
  p (The page for this request is not impletmented yet)
  table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
    tbody
      tr
        td(align='left')
          table(role='presentation', border='0', cellpadding='0', cellspacing='0')
            tbody
              tr
                td
                  a(href=url, target='_blank') Reset your password
  p If you did't forgot your password then ignore this email
